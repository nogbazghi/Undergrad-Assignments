
def termFreq(document,word):
    dictionary = {}
    for line in document:
        l = line.split
        for i in l:
            if i not in dictionary:
                dict[i]= 1
            else:
                dict[i] += 1
    return dictionary

def docfreq(list):
    #list of dictionaries
    for i in list:
        for d in list:
            if i != d:
                for word in dict:
                    if list[i][word] == list[d][word]:

